#!/usr/bin/env bash

color=\#717171
vpnstatus=""

if nordvpn status | grep "Status: Connected" ; then
    color=\#15d4ce
    server=$(nordvpn status | grep "Hostname:" | sed -e "s/Hostname: //" | grep -oE "^[^.]+")
    vpnstatus="[$server] "
fi

echo "$vpnstatus"
echo
echo $color
